# 使用 code-server(VS Code) Web IDE

1. 啟動 VS Code Web UI (code-server)

   ```
   kde code-server [options]

   options:
       -d : 背景執行
       -p : 指定 Port (預設 8080)
   ```

2. 啟動瀏覽器，打開 http://127.0.0.1:8080 or http://127.0.0.1:[port]
